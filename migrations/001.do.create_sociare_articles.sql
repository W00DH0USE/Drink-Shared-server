CREATE TABLE sociare_articles (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  title TEXT NOT NULL,
  style TEXT NOT NULL,
  ingredients TEXT NOT NULL,
  directions TEXT NOT NULL,
  date_created TIMESTAMP DEFAULT now() NOT NULL
);
